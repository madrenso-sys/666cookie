body {
    display: flex;
    flex-direction: column;
    align-items: center;
    font-family: sans-serif;
    background-color: #333; /* 짙은 회색 배경 */
    color: #f0f0f0; 
    margin: 0;
    padding: 20px 0;
    min-height: 100vh;
}

h1 {
    margin-bottom: 30px;
    font-size: 2em;
}

.card-container {
    display: flex;
    flex-wrap: wrap; 
    gap: 25px; 
    width: 90vw; 
    max-width: 1000px; 
    justify-content: center;
}

.card {
    width: 18vw; 
    max-width: 200px; 
    height: calc(18vw * 4 / 3); /* 3:4 비율 */
    max-height: calc(200px * 4 / 3);
    perspective: 1000px; 
    cursor: pointer;
    /* 흩어지는 애니메이션 속도 설정 (1초) */
    transition: transform 1s ease-out, opacity 1s ease-out; 
    opacity: 1; 
}

/* 🔄 초기 로딩 시 카드를 중앙에 고정시키는 스타일 (시각적 섞임의 시작 위치) */
.card.initial-shuffle {
    opacity: 0; 
    position: absolute; 
    top: 50%;
    left: 50%;
    margin-top: calc(-18vw * 4 / 3 / 2); 
    margin-left: calc(-18vw / 2);
}


.card-inner {
    position: relative;
    width: 100%;
    height: 100%;
    text-align: center;
    transition: transform 0.8s; /* 뒤집는 애니메이션 시간 */
    transform-style: preserve-3d; 
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.5); 
    border-radius: 12px;
}

.card.is-flipped .card-inner {
    transform: rotateY(180deg);
}

.card-face {
    position: absolute;
    width: 100%;
    height: 100%;
    backface-visibility: hidden; 
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 12px;
    
    background-size: cover; 
    background-position: center; 
    color: transparent; 
    font-size: 30px;
    font-weight: bold;
}

/* 🎨 카드 앞면 (공통 PNG 이미지 경로) */
.card-front {
    background-image: url('images/card_front_common.png'); 
    background-color: #4A89DC; 
}

.card-back {
    background-color: #A0D468; 
    transform: rotateY(180deg);
}

@media (max-width: 600px) {
    .card {
        width: 40vw; 
        height: calc(40vw * 4 / 3);
    }
    .card.initial-shuffle {
        margin-top: calc(-40vw * 4 / 3 / 2);
        margin-left: calc(-40vw / 2);
    }
}